export enum Role {
  Admin = 'admin',
  Volunteer = 'volunteer',
  Participant = 'participant',
  Unknown = 'unknown',
}

export enum CampaignStatus {
  Current = 'current',
  Upcoming = 'upcoming',
  Past = 'past',
}
